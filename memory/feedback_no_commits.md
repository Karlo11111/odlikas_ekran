---
name: No commits during implementation
description: User handles all git commits themselves at end of session — never commit mid-implementation
type: feedback
---

Never run git commit or git push during implementation. The user stages and commits everything themselves at the end.

**Why:** User preference — explicit instruction given when I tried to commit mid-task.

**How to apply:** Skip all commit steps in plans. Implement all files, then stop. Don't touch git at all.
